local config = {
  iris = colors.lightGray,
  background = colors.black,
  gatering = colors.gray,
  eventhorizont = colors.blue,
  text = colors.white,
  chevronMW = colors.orange,
  chevronPG = colors.blue,
  chevronUN = colors.white,
  dialButton = colors.orange,
  addButton = colors.lightBlue,
  editButton = colors.cyan,
  exitButton = colors.red
}
return config